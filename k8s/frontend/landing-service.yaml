apiVersion: v1
kind: Service
metadata:
  name: landing-service
  namespace: codesync
spec:
  selector:
    app: landing
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30001
  type: NodePort
