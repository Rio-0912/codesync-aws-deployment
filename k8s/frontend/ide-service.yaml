apiVersion: v1
kind: Service
metadata:
  name: ide-service
  namespace: codesync
spec:
  selector:
    app: ide
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30002
  type: NodePort
