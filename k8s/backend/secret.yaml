apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: codesync
type: Opaque
data:
  # The actual SSH private key will be injected manually or generated.
  # This is a placeholder for `ssh-keygen` generated key base64 encoded.
  # Use base64 string for empty key for now.
  SSH_PRIVATE_KEY: ""
