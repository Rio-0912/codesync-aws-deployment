apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: codesync
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
      nodePort: 30003
  type: NodePort
  sessionAffinity: ClientIP
